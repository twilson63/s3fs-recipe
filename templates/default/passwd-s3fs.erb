<% @buckets.each do |bucket| %>
<%= bucket[:name] %>:<%= ( bucket[:access_key_id] == '' ? Chef::EncryptedDataBagItem.load("aws_users", "s3_user")["aws_access_key"] : bucket[:access_key_id] ) %>:<%= ( bucket[:secret_access_key] == '' ? Chef::EncryptedDataBagItem.load("aws_users", "s3_user")["aws_secret_key"] : bucket[:secret_access_key] ) %>
<% end %>
